[pytest]
testpaths = shared/tests services tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:shared.*

asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

markers =
    integration: needs a live PostgreSQL; auto-skipped when none is reachable
